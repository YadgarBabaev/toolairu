{% extends '::adminBase.html.twig' %}
{% block content_header %}
    <h1>
        Каталог
        <small>Склады</small>
    </h1>
{% endblock content_header %}
{% block content %}
    <div class="box box-info">
        <div class="box-header with-border">
            <a class="btn btn-primary" href="{{ path('admin_shop_store_new') }}">
                Добавить <i class="fa fa-fw fa-plus-square-o"></i>
            </a>
        </div>
        <table class="table table-bordered">
            <thead>
            <tr>
                <th>Id</th>
                <th>Наименование</th>
                <th>Действия</th>
            </tr>
            </thead>
            <tbody>
            {% for store in stores %}
                <tr>
                    <td>{{ store.id }}</td>
                    <td><a href="{{ path('admin_shop_store_edit', { 'id': store.id }) }}">{{ store }}</a></td>
                    <td>
                        <a href="{{ path('admin_shop_product_store_list',{ 'id': store.id }) }}" class="btn btn-default">Товары</a>
                    </td>
                    <td>
                        <a class="btn btn-danger" href="{{ path('admin_shop_store_delete', { 'id': store.id }) }}">Удалить <i class="fa fa-fw fa-minus-square-o"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
